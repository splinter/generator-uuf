<% if ((hasLayout) && (layoutName)) { %>{{layout "<%= layoutName %>"}}<% } %>

{{#zone "main"}}

	{{!--
	Define the unit view here.

	The view is able to access any property sent by the view controller script
	or passed in through the unit helper (e.g. {{unit "unit-name" title="Hello World!"}})
	--}}

{{/zone}}